// generated by dlavm.backend in 2026-03-24 10:20:04

// hbm storage define
uint64_t hbm0 = 0x000000000;
uint64_t hbm1 = 0x000006900;
uint64_t hbm2 = 0x0000069c0;
uint64_t hbm3 = 0x000006a80;
uint64_t hbm4 = 0x000030a80;
uint64_t hbm5 = 0x000030e80;
uint64_t hbm6 = 0x000040e80;
uint64_t hbm7 = 0x00004b680;
uint64_t hbm8 = 0x00004b780;
uint64_t hbm9 = 0x0000ed780;
uint64_t hbm10 = 0x0000f7f80;
uint64_t hbm11 = 0x0000f8080;
uint64_t hbm12 = 0x000121480;
uint64_t hbm13 = 0x000121540;
uint64_t hbm14 = 0x000121600;
uint64_t hbm15 = 0x000133180;
uint64_t hbm16 = 0x000133340;
uint64_t hbm17 = 0x0001333c0;
uint64_t hbm18 = 0x000144f40;
uint64_t hbm19 = 0x000145100;
uint64_t hbm20 = 0x000156900;
uint64_t hbm21 = 0x0001569c0;
uint64_t hbm22 = 0x000156a80;
uint64_t hbm23 = 0x000180a80;
uint64_t hbm24 = 0x000180e80;
uint64_t hbm25 = 0x00018b680;
uint64_t hbm26 = 0x00018b780;
uint64_t hbm27 = 0x00022d780;
uint64_t hbm28 = 0x000237f80;
uint64_t hbm29 = 0x000238080;
uint64_t hbm30 = 0x000261480;
uint64_t hbm31 = 0x000261540;
uint64_t hbm32 = 0x000261600;
uint64_t hbm33 = 0x000273180;
uint64_t hbm34 = 0x000273340;
uint64_t hbm35 = 0x000284ec0;
uint64_t hbm36 = 0x000285080;
uint64_t hbm37 = 0x000296880;
uint64_t hbm38 = 0x000296940;
uint64_t hbm39 = 0x000296a00;
uint64_t hbm40 = 0x0002c0a00;
uint64_t hbm41 = 0x0002c0e00;
uint64_t hbm42 = 0x0002cb600;
uint64_t hbm43 = 0x0002cb700;
uint64_t hbm44 = 0x00036d700;
uint64_t hbm45 = 0x000377f00;
uint64_t hbm46 = 0x000378000;
uint64_t hbm47 = 0x0003a1400;
uint64_t hbm48 = 0x0003a14c0;
uint64_t hbm49 = 0x0003a1580;
uint64_t hbm50 = 0x0003b3100;
uint64_t hbm51 = 0x0003b32c0;
uint64_t hbm52 = 0x0003c4e40;
uint64_t hbm53 = 0x0003c5000;
uint64_t hbm54 = 0x0003d6800;
uint64_t hbm55 = 0x0003d68c0;
uint64_t hbm56 = 0x0003d6980;
uint64_t hbm57 = 0x000400980;
uint64_t hbm58 = 0x000400d80;
uint64_t hbm59 = 0x00040b580;
uint64_t hbm60 = 0x00040b680;
uint64_t hbm61 = 0x0004ad680;
uint64_t hbm62 = 0x0004b7e80;
uint64_t hbm63 = 0x0004b7f80;
uint64_t hbm64 = 0x0004e1380;
uint64_t hbm65 = 0x0004e1440;
uint64_t hbm66 = 0x0004e1500;
uint64_t hbm67 = 0x0004f3080;
uint64_t hbm68 = 0x0004f3240;
uint64_t hbm69 = 0x000504dc0;
uint64_t hbm70 = 0x000504f80;
uint64_t hbm71 = 0x000516780;
uint64_t hbm72 = 0x000516840;
uint64_t hbm73 = 0x000516900;
uint64_t hbm74 = 0x000540900;
uint64_t hbm75 = 0x000540d00;
uint64_t hbm76 = 0x00054b500;
uint64_t hbm77 = 0x00054b600;
uint64_t hbm78 = 0x0005ed600;
uint64_t hbm79 = 0x0005f7e00;
uint64_t hbm80 = 0x0005f7f00;
uint64_t hbm81 = 0x000621300;
uint64_t hbm82 = 0x0006213c0;
uint64_t hbm83 = 0x000621480;
uint64_t hbm84 = 0x000633000;
uint64_t hbm85 = 0x0006331c0;
uint64_t hbm86 = 0x000644d40;
uint64_t hbm87 = 0x000644f00;
uint64_t hbm88 = 0x000656700;
uint64_t hbm89 = 0x0006567c0;
uint64_t hbm90 = 0x000656880;
uint64_t hbm91 = 0x000680880;
uint64_t hbm92 = 0x000680c80;
uint64_t hbm93 = 0x00068b480;
uint64_t hbm94 = 0x00068b580;
uint64_t hbm95 = 0x00072d580;
uint64_t hbm96 = 0x000737d80;
uint64_t hbm97 = 0x000737e80;
uint64_t hbm98 = 0x000761280;
uint64_t hbm99 = 0x000761340;
uint64_t hbm100 = 0x000761400;
uint64_t hbm101 = 0x000772f80;
uint64_t hbm102 = 0x000773140;
uint64_t hbm103 = 0x000784cc0;
uint64_t hbm104 = 0x000784e80;
uint64_t hbm105 = 0x000796680;
uint64_t hbm106 = 0x000796740;
uint64_t hbm107 = 0x000796800;
uint64_t hbm108 = 0x0007c0800;
uint64_t hbm109 = 0x0007c0c00;
uint64_t hbm110 = 0x0007cb400;
uint64_t hbm111 = 0x0007cb500;
uint64_t hbm112 = 0x00086d500;
uint64_t hbm113 = 0x000877d00;
uint64_t hbm114 = 0x000877e00;
uint64_t hbm115 = 0x0008a1200;
uint64_t hbm116 = 0x0008a12c0;
uint64_t hbm117 = 0x0008a1380;
uint64_t hbm118 = 0x0008b2f00;
uint64_t hbm119 = 0x0008b30c0;
uint64_t hbm120 = 0x0008c4c40;
uint64_t hbm121 = 0x0008c4e00;
uint64_t hbm122 = 0x0008d6600;
uint64_t hbm123 = 0x0008d66c0;
uint64_t hbm124 = 0x0008d6780;
uint64_t hbm125 = 0x000900780;
uint64_t hbm126 = 0x000900b80;
uint64_t hbm127 = 0x00090b380;
uint64_t hbm128 = 0x00090b480;
uint64_t hbm129 = 0x0009ad480;
uint64_t hbm130 = 0x0009b7c80;
uint64_t hbm131 = 0x0009b7d80;
uint64_t hbm132 = 0x0009e1180;
uint64_t hbm133 = 0x0009e1240;
uint64_t hbm134 = 0x0009e1300;
uint64_t hbm135 = 0x0009f2e80;
uint64_t hbm136 = 0x0009f3040;
uint64_t hbm137 = 0x000a04bc0;
uint64_t hbm138 = 0x000a04d80;
uint64_t hbm139 = 0x000a16580;
uint64_t hbm140 = 0x000a16640;
uint64_t hbm141 = 0x000a16700;
uint64_t hbm142 = 0x000a40700;
uint64_t hbm143 = 0x000a40b00;
uint64_t hbm144 = 0x000a4b300;
uint64_t hbm145 = 0x000a4b400;
uint64_t hbm146 = 0x000aed400;
uint64_t hbm147 = 0x000af7c00;
uint64_t hbm148 = 0x000af7d00;
uint64_t hbm149 = 0x000b21100;
uint64_t hbm150 = 0x000b211c0;
uint64_t hbm151 = 0x000b21280;
uint64_t hbm152 = 0x000b32e00;
uint64_t hbm153 = 0x000b32fc0;
uint64_t hbm154 = 0x000b44b40;
uint64_t hbm155 = 0x000b44d00;
uint64_t hbm156 = 0x000b56500;
uint64_t hbm157 = 0x000b565c0;
uint64_t hbm158 = 0x000b56680;
uint64_t hbm159 = 0x000b80680;
uint64_t hbm160 = 0x000b80a80;
uint64_t hbm161 = 0x000b8b280;
uint64_t hbm162 = 0x000b8b380;
uint64_t hbm163 = 0x000c2d380;
uint64_t hbm164 = 0x000c37b80;
uint64_t hbm165 = 0x000c37c80;
uint64_t hbm166 = 0x000c61080;
uint64_t hbm167 = 0x000c61140;
uint64_t hbm168 = 0x000c61200;
uint64_t hbm169 = 0x000c72d80;
uint64_t hbm170 = 0x000c72f40;
uint64_t hbm171 = 0x000c84ac0;
uint64_t hbm172 = 0x000c84c80;
uint64_t hbm173 = 0x000c96480;
uint64_t hbm174 = 0x000c96540;
uint64_t hbm175 = 0x000c96600;
uint64_t hbm176 = 0x000cc0600;
uint64_t hbm177 = 0x000cc0a00;
uint64_t hbm178 = 0x000ccb200;
uint64_t hbm179 = 0x000ccb300;
uint64_t hbm180 = 0x000d6d300;
uint64_t hbm181 = 0x000d77b00;
uint64_t hbm182 = 0x000d77c00;
uint64_t hbm183 = 0x000da1000;
uint64_t hbm184 = 0x000da10c0;
uint64_t hbm185 = 0x000da1180;
uint64_t hbm186 = 0x000db2d00;
uint64_t hbm187 = 0x000db2ec0;
uint64_t hbm188 = 0x000dc4a40;
uint64_t hbm189 = 0x000dc4c00;
uint64_t hbm190 = 0x000dd6400;
uint64_t hbm191 = 0x000dd64c0;
uint64_t hbm192 = 0x000dd6580;
uint64_t hbm193 = 0x000e00580;
uint64_t hbm194 = 0x000e00980;
uint64_t hbm195 = 0x000e0b180;
uint64_t hbm196 = 0x000e0b280;
uint64_t hbm197 = 0x000ead280;
uint64_t hbm198 = 0x000eb7a80;
uint64_t hbm199 = 0x000eb7b80;
uint64_t hbm200 = 0x000ee0f80;
uint64_t hbm201 = 0x000ee1040;
uint64_t hbm202 = 0x000ee1100;
uint64_t hbm203 = 0x000ef2c80;
uint64_t hbm204 = 0x000ef2e40;
uint64_t hbm205 = 0x000f049c0;
uint64_t hbm206 = 0x000f04b80;
uint64_t hbm207 = 0x000f16380;
uint64_t hbm208 = 0x000f16440;
uint64_t hbm209 = 0x000f16500;
uint64_t hbm210 = 0x000f40500;
uint64_t hbm211 = 0x000f40900;
uint64_t hbm212 = 0x000f4b100;
uint64_t hbm213 = 0x000f4b200;
uint64_t hbm214 = 0x000fed200;
uint64_t hbm215 = 0x000ff7a00;
uint64_t hbm216 = 0x000ff7b00;
uint64_t hbm217 = 0x001020f00;
uint64_t hbm218 = 0x001020fc0;
uint64_t hbm219 = 0x001021080;
uint64_t hbm220 = 0x001032c00;
uint64_t hbm221 = 0x001032dc0;
uint64_t hbm222 = 0x001044940;
uint64_t hbm223 = 0x001044b00;
uint64_t hbm224 = 0x001056300;
uint64_t hbm225 = 0x0010563c0;
uint64_t hbm226 = 0x001056480;
uint64_t hbm227 = 0x001080480;
uint64_t hbm228 = 0x001080880;
uint64_t hbm229 = 0x00108b080;
uint64_t hbm230 = 0x00108b180;
uint64_t hbm231 = 0x00112d180;
uint64_t hbm232 = 0x001137980;
uint64_t hbm233 = 0x001137a80;
uint64_t hbm234 = 0x001160e80;
uint64_t hbm235 = 0x001160f40;
uint64_t hbm236 = 0x001161000;
uint64_t hbm237 = 0x001172b80;
uint64_t hbm238 = 0x001172d40;
uint64_t hbm239 = 0x0011848c0;
uint64_t hbm240 = 0x001184a80;
uint64_t hbm241 = 0x001196280;
uint64_t hbm242 = 0x001196340;
uint64_t hbm243 = 0x001196400;
uint64_t hbm244 = 0x0011c0400;
uint64_t hbm245 = 0x0011c0800;
uint64_t hbm246 = 0x0011cb000;
uint64_t hbm247 = 0x0011cb100;
uint64_t hbm248 = 0x00126d100;
uint64_t hbm249 = 0x001277900;
uint64_t hbm250 = 0x001277a00;
uint64_t hbm251 = 0x0012a0e00;
uint64_t hbm252 = 0x0012a0ec0;
uint64_t hbm253 = 0x0012a0f80;
uint64_t hbm254 = 0x0012b2b00;
uint64_t hbm255 = 0x0012b2cc0;
uint64_t hbm256 = 0x0012c4840;
uint64_t hbm257 = 0x0012c4a00;
uint64_t hbm258 = 0x0012d6200;
uint64_t hbm259 = 0x0012d62c0;
uint64_t hbm260 = 0x0012d6380;
uint64_t hbm261 = 0x001300380;
uint64_t hbm262 = 0x001300780;
uint64_t hbm263 = 0x00130af80;
uint64_t hbm264 = 0x00130b080;
uint64_t hbm265 = 0x0013ad080;
uint64_t hbm266 = 0x0013b7880;
uint64_t hbm267 = 0x0013b7980;
uint64_t hbm268 = 0x0013e0d80;
uint64_t hbm269 = 0x0013e0e40;
uint64_t hbm270 = 0x0013e0f00;
uint64_t hbm271 = 0x0013f2a80;
uint64_t hbm272 = 0x0013f2c40;
uint64_t hbm273 = 0x0014047c0;
uint64_t hbm274 = 0x001404980;
uint64_t hbm275 = 0x001416180;
uint64_t hbm276 = 0x001416240;
uint64_t hbm277 = 0x001416300;
uint64_t hbm278 = 0x001440300;
uint64_t hbm279 = 0x001440700;
uint64_t hbm280 = 0x00144af00;
uint64_t hbm281 = 0x00144b000;
uint64_t hbm282 = 0x0014ed000;
uint64_t hbm283 = 0x0014f7800;
uint64_t hbm284 = 0x0014f7900;
uint64_t hbm285 = 0x001520d00;
uint64_t hbm286 = 0x001520dc0;
uint64_t hbm287 = 0x001520e80;
uint64_t hbm288 = 0x001532a00;
uint64_t hbm289 = 0x001532bc0;
uint64_t hbm290 = 0x001544740;
uint64_t hbm291 = 0x001544900;
uint64_t hbm292 = 0x001556100;
uint64_t hbm293 = 0x0015561c0;
uint64_t hbm294 = 0x001556280;
uint64_t hbm295 = 0x001580280;
uint64_t hbm296 = 0x001580680;
uint64_t hbm297 = 0x00158ae80;
uint64_t hbm298 = 0x00158af80;
uint64_t hbm299 = 0x00162cf80;
uint64_t hbm300 = 0x001637780;
uint64_t hbm301 = 0x001637880;
uint64_t hbm302 = 0x001660c80;
uint64_t hbm303 = 0x001660d40;
uint64_t hbm304 = 0x001660e00;
uint64_t hbm305 = 0x001672980;
uint64_t hbm306 = 0x001672b40;
uint64_t hbm307 = 0x0016846c0;
uint64_t hbm308 = 0x001684880;
uint64_t hbm309 = 0x001696080;
uint64_t hbm310 = 0x001696140;
uint64_t hbm311 = 0x001696200;
uint64_t hbm312 = 0x0016c0200;
uint64_t hbm313 = 0x0016c0600;
uint64_t hbm314 = 0x0016cae00;
uint64_t hbm315 = 0x0016caf00;
uint64_t hbm316 = 0x00176cf00;
uint64_t hbm317 = 0x001777700;
uint64_t hbm318 = 0x001777800;
uint64_t hbm319 = 0x0017a0c00;
uint64_t hbm320 = 0x0017a0cc0;
uint64_t hbm321 = 0x0017a0d80;
uint64_t hbm322 = 0x0017b2900;
uint64_t hbm323 = 0x0017b2ac0;
uint64_t hbm324 = 0x0017c4640;
uint64_t hbm325 = 0x0017c4800;
uint64_t hbm326 = 0x0017d6000;
uint64_t hbm327 = 0x0017d60c0;
uint64_t hbm328 = 0x0017d6180;
uint64_t hbm329 = 0x001800180;
uint64_t hbm330 = 0x001800580;
uint64_t hbm331 = 0x00180ad80;
uint64_t hbm332 = 0x00180ae80;
uint64_t hbm333 = 0x0018ace80;
uint64_t hbm334 = 0x0018b7680;
uint64_t hbm335 = 0x0018b7780;
uint64_t hbm336 = 0x0018e0b80;
uint64_t hbm337 = 0x0018e0c40;
uint64_t hbm338 = 0x0018e0d00;
uint64_t hbm339 = 0x0018f2880;
uint64_t hbm340 = 0x0018f2a40;
uint64_t hbm341 = 0x0019045c0;
uint64_t hbm342 = 0x001904780;
uint64_t hbm343 = 0x001915f80;
uint64_t hbm344 = 0x001916040;
uint64_t hbm345 = 0x001916100;
uint64_t hbm346 = 0x001940100;
uint64_t hbm347 = 0x001940500;
uint64_t hbm348 = 0x00194ad00;
uint64_t hbm349 = 0x00194ae00;
uint64_t hbm350 = 0x0019ece00;
uint64_t hbm351 = 0x0019f7600;
uint64_t hbm352 = 0x0019f7700;
uint64_t hbm353 = 0x001a20b00;
uint64_t hbm354 = 0x001a20bc0;
uint64_t hbm355 = 0x001a20c80;
uint64_t hbm356 = 0x001a32800;
uint64_t hbm357 = 0x001a329c0;
uint64_t hbm358 = 0x001a44540;
uint64_t hbm359 = 0x001a44700;
uint64_t hbm360 = 0x001a55f00;
uint64_t hbm361 = 0x001a55fc0;
uint64_t hbm362 = 0x001a56080;
uint64_t hbm363 = 0x001a80080;
uint64_t hbm364 = 0x001a80480;
uint64_t hbm365 = 0x001a8ac80;
uint64_t hbm366 = 0x001a8ad80;
uint64_t hbm367 = 0x001b2cd80;
uint64_t hbm368 = 0x001b37580;
uint64_t hbm369 = 0x001b37680;
uint64_t hbm370 = 0x001b60a80;
uint64_t hbm371 = 0x001b60b40;
uint64_t hbm372 = 0x001b60c00;
uint64_t hbm373 = 0x001b72780;
uint64_t hbm374 = 0x001b72940;
uint64_t hbm375 = 0x001b844c0;
uint64_t hbm376 = 0x001b84680;
uint64_t hbm377 = 0x001b95e80;
uint64_t hbm378 = 0x001b95f40;
uint64_t hbm379 = 0x001b96000;
uint64_t hbm380 = 0x001bc0000;
uint64_t hbm381 = 0x001bc0400;
uint64_t hbm382 = 0x001bcac00;
uint64_t hbm383 = 0x001bcad00;
uint64_t hbm384 = 0x001c6cd00;
uint64_t hbm385 = 0x001c77500;
uint64_t hbm386 = 0x001c77600;
uint64_t hbm387 = 0x001ca0a00;
uint64_t hbm388 = 0x001ca0ac0;
uint64_t hbm389 = 0x001ca0b80;
uint64_t hbm390 = 0x001cb2700;
uint64_t hbm391 = 0x001cb28c0;
uint64_t hbm392 = 0x001cc4440;
uint64_t hbm393 = 0x001cc4600;
uint64_t hbm394 = 0x001cd5e00;
uint64_t hbm395 = 0x001cd5ec0;
uint64_t hbm396 = 0x001cd5f80;
uint64_t hbm397 = 0x001cfff80;
uint64_t hbm398 = 0x001d00380;
uint64_t hbm399 = 0x001d0ab80;
uint64_t hbm400 = 0x001d0ac80;
uint64_t hbm401 = 0x001dacc80;
uint64_t hbm402 = 0x001db7480;
uint64_t hbm403 = 0x001db7580;
uint64_t hbm404 = 0x001de0980;
uint64_t hbm405 = 0x001de0a40;
uint64_t hbm406 = 0x001de0b00;
uint64_t hbm407 = 0x001df2680;
uint64_t hbm408 = 0x001df2840;
uint64_t hbm409 = 0x001e043c0;
uint64_t hbm410 = 0x001e04580;
uint64_t hbm411 = 0x001e15d80;
uint64_t hbm412 = 0x001e15e40;
uint64_t hbm413 = 0x001e15f00;
uint64_t hbm414 = 0x001e3ff00;
uint64_t hbm415 = 0x001e40300;
uint64_t hbm416 = 0x001e4ab00;
uint64_t hbm417 = 0x001e4ac00;
uint64_t hbm418 = 0x001eecc00;
uint64_t hbm419 = 0x001ef7400;
uint64_t hbm420 = 0x001ef7500;
uint64_t hbm421 = 0x001f20900;
uint64_t hbm422 = 0x001f209c0;
uint64_t hbm423 = 0x001f20a80;
uint64_t hbm424 = 0x001f32600;
uint64_t hbm425 = 0x001f327c0;
uint64_t hbm426 = 0x001f44340;
uint64_t hbm427 = 0x001f44500;
uint64_t hbm428 = 0x001f55d00;
uint64_t hbm429 = 0x001f55dc0;
uint64_t hbm430 = 0x001f55e80;
uint64_t hbm431 = 0x001f7fe80;
uint64_t hbm432 = 0x001f80280;
uint64_t hbm433 = 0x001f8aa80;
uint64_t hbm434 = 0x001f8ab80;
uint64_t hbm435 = 0x00202cb80;
uint64_t hbm436 = 0x002037380;
uint64_t hbm437 = 0x002037480;
uint64_t hbm438 = 0x002060880;
uint64_t hbm439 = 0x002060940;
uint64_t hbm440 = 0x002060a00;
uint64_t hbm441 = 0x002072580;
uint64_t hbm442 = 0x002072740;
uint64_t hbm443 = 0x0020842c0;
uint64_t hbm444 = 0x002084480;
uint64_t hbm445 = 0x002095c80;
uint64_t hbm446 = 0x002095d40;
uint64_t hbm447 = 0x002095e00;
uint64_t hbm448 = 0x0020bfe00;
uint64_t hbm449 = 0x0020c0200;
uint64_t hbm450 = 0x0020caa00;
uint64_t hbm451 = 0x0020cab00;
uint64_t hbm452 = 0x00216cb00;
uint64_t hbm453 = 0x002177300;
uint64_t hbm454 = 0x002177400;
uint64_t hbm455 = 0x0021a0800;
uint64_t hbm456 = 0x0021a08c0;
uint64_t hbm457 = 0x0021a0980;
uint64_t hbm458 = 0x0021b2500;
uint64_t hbm459 = 0x0021b26c0;
uint64_t hbm460 = 0x0021c4240;
uint64_t hbm461 = 0x0021c4400;
uint64_t hbm462 = 0x0021d5c00;
uint64_t hbm463 = 0x0021d5cc0;
uint64_t hbm464 = 0x0021d5d80;
uint64_t hbm465 = 0x0021ffd80;
uint64_t hbm466 = 0x002200180;
uint64_t hbm467 = 0x00220a980;
uint64_t hbm468 = 0x00220aa80;
uint64_t hbm469 = 0x0022aca80;
uint64_t hbm470 = 0x0022b7280;
uint64_t hbm471 = 0x0022b7380;
uint64_t hbm472 = 0x0022e0780;
uint64_t hbm473 = 0x0022e0840;
uint64_t hbm474 = 0x0022e0900;
uint64_t hbm475 = 0x0022f2480;
uint64_t hbm476 = 0x0022f2640;
uint64_t hbm477 = 0x0023041c0;
uint64_t hbm478 = 0x002304380;
uint64_t hbm479 = 0x002315b80;
uint64_t hbm480 = 0x002315c40;
uint64_t hbm481 = 0x002315d00;
uint64_t hbm482 = 0x00233fd00;
uint64_t hbm483 = 0x002340100;
uint64_t hbm484 = 0x00234a900;
uint64_t hbm485 = 0x00234aa00;
uint64_t hbm486 = 0x0023eca00;
uint64_t hbm487 = 0x0023f7200;
uint64_t hbm488 = 0x0023f7300;
uint64_t hbm489 = 0x002420700;
uint64_t hbm490 = 0x0024207c0;
uint64_t hbm491 = 0x002420880;
uint64_t hbm492 = 0x002432400;
uint64_t hbm493 = 0x0024325c0;
uint64_t hbm494 = 0x002444140;
uint64_t hbm495 = 0x002444300;
uint64_t hbm496 = 0x002455b00;
uint64_t hbm497 = 0x002455bc0;
uint64_t hbm498 = 0x002455c80;
uint64_t hbm499 = 0x00247fc80;
uint64_t hbm500 = 0x002480080;
uint64_t hbm501 = 0x00248a880;
uint64_t hbm502 = 0x00248a980;
uint64_t hbm503 = 0x00252c980;
uint64_t hbm504 = 0x002537180;
uint64_t hbm505 = 0x002537280;
uint64_t hbm506 = 0x002560680;
uint64_t hbm507 = 0x002560740;
uint64_t hbm508 = 0x002560800;
uint64_t hbm509 = 0x002572380;
uint64_t hbm510 = 0x002572540;
uint64_t hbm511 = 0x0025840c0;
uint64_t hbm512 = 0x002584280;
uint64_t hbm513 = 0x002595a80;
uint64_t hbm514 = 0x002595b40;
uint64_t hbm515 = 0x002595c00;
uint64_t hbm516 = 0x0025bfc00;
uint64_t hbm517 = 0x0025c0000;
uint64_t hbm518 = 0x0025ca800;
uint64_t hbm519 = 0x0025ca900;
uint64_t hbm520 = 0x00266c900;
uint64_t hbm521 = 0x002677100;
uint64_t hbm522 = 0x002677200;
uint64_t hbm523 = 0x0026a0600;
uint64_t hbm524 = 0x0026a06c0;
uint64_t hbm525 = 0x0026a0780;
uint64_t hbm526 = 0x0026b2300;
uint64_t hbm527 = 0x0026b24c0;
uint64_t hbm528 = 0x0026c4040;
uint64_t hbm529 = 0x0026c4200;
uint64_t hbm530 = 0x0026d5a00;
uint64_t hbm531 = 0x0026d5ac0;
uint64_t hbm532 = 0x0026d5b80;
uint64_t hbm533 = 0x0026ffb80;
uint64_t hbm534 = 0x0026fff80;
uint64_t hbm535 = 0x00270a780;
uint64_t hbm536 = 0x00270a880;
uint64_t hbm537 = 0x0027ac880;
uint64_t hbm538 = 0x0027b7080;
uint64_t hbm539 = 0x0027b7180;
uint64_t hbm540 = 0x0027e0580;
uint64_t hbm541 = 0x0027e0640;
uint64_t hbm542 = 0x0027e0700;
uint64_t hbm543 = 0x0027f2280;
uint64_t hbm544 = 0x0027f2440;
uint64_t hbm545 = 0x002803fc0;
uint64_t hbm546 = 0x002804180;
uint64_t hbm547 = 0x002815980;
// hbm_cache storage define
uint64_t hbm_cache0 = 0x002815a40;
uint64_t hbm_cache1 = 0x002855a40;
uint64_t hbm_cache2 = 0x002895a40;
uint64_t hbm_cache3 = 0x0028d5a40;
uint64_t hbm_cache4 = 0x002915a40;
uint64_t hbm_cache5 = 0x002955a40;
uint64_t hbm_cache6 = 0x002995a40;
uint64_t hbm_cache7 = 0x0029d5a40;
uint64_t hbm_cache8 = 0x002a15a40;
uint64_t hbm_cache9 = 0x002a55a40;
uint64_t hbm_cache10 = 0x002a95a40;
uint64_t hbm_cache11 = 0x002ad5a40;
uint64_t hbm_cache12 = 0x002b15a40;
uint64_t hbm_cache13 = 0x002b55a40;
uint64_t hbm_cache14 = 0x002b95a40;
uint64_t hbm_cache15 = 0x002bd5a40;
uint64_t hbm_cache16 = 0x002c15a40;
uint64_t hbm_cache17 = 0x002c55a40;
uint64_t hbm_cache18 = 0x002c95a40;
uint64_t hbm_cache19 = 0x002cd5a40;
uint64_t hbm_cache20 = 0x002d15a40;
uint64_t hbm_cache21 = 0x002d55a40;
uint64_t hbm_cache22 = 0x002d95a40;
uint64_t hbm_cache23 = 0x002dd5a40;
uint64_t hbm_cache24 = 0x002e15a40;
uint64_t hbm_cache25 = 0x002e55a40;
uint64_t hbm_cache26 = 0x002e95a40;
uint64_t hbm_cache27 = 0x002ed5a40;
uint64_t hbm_cache28 = 0x002f15a40;
uint64_t hbm_cache29 = 0x002f55a40;
uint64_t hbm_cache30 = 0x002f95a40;
uint64_t hbm_cache31 = 0x002fd5a40;
uint64_t hbm_cache32 = 0x003015a40;
uint64_t hbm_cache33 = 0x003055a40;
uint64_t hbm_cache34 = 0x003095a40;
uint64_t hbm_cache35 = 0x0030d5a40;
uint64_t hbm_cache36 = 0x003115a40;
uint64_t hbm_cache37 = 0x003155a40;
uint64_t hbm_cache38 = 0x003195a40;
uint64_t hbm_cache39 = 0x0031d5a40;
uint64_t hbm_cache40 = 0x003215a40;
uint64_t hbm_cache41 = 0x003255a40;
uint64_t hbm_cache42 = 0x003295a40;
uint64_t hbm_cache43 = 0x0032d5a40;
uint64_t hbm_cache44 = 0x003315a40;
uint64_t hbm_cache45 = 0x003355a40;
uint64_t hbm_cache46 = 0x003395a40;
uint64_t hbm_cache47 = 0x0033d5a40;
uint64_t hbm_cache48 = 0x003415a40;
uint64_t hbm_cache49 = 0x003455a40;
uint64_t hbm_cache50 = 0x003495a40;
uint64_t hbm_cache51 = 0x0034d5a40;
uint64_t hbm_cache52 = 0x003515a40;
uint64_t hbm_cache53 = 0x003555a40;
uint64_t hbm_cache54 = 0x003595a40;
uint64_t hbm_cache55 = 0x0035d5a40;
uint64_t hbm_cache56 = 0x003615a40;
uint64_t hbm_cache57 = 0x003655a40;
uint64_t hbm_cache58 = 0x003695a40;
uint64_t hbm_cache59 = 0x0036d5a40;
uint64_t hbm_cache60 = 0x003715a40;
uint64_t hbm_cache61 = 0x003755a40;
uint64_t hbm_cache62 = 0x003795a40;
uint64_t hbm_cache63 = 0x0037d5a40;
// runtime storage define
uint64_t runtime0 = 0x003815a40;
uint64_t runtime1 = 0x003845a40;
uint64_t runtime2 = 0x0038b5a40;
uint64_t runtime3 = 0x0040b5a40;
uint64_t runtime4 = 0x0048b5a40;

uint64_t raw_image_in = runtime0;

uint64_t output = runtime1;

void wall_oss_run_CONV3D_visual_patch_embed_projpatch_embed_wt_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/CONV3D_visual_patch_embed_projMVM_BN_write_to_HBM_bin/MVMCONV3D_HBM_DDR_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm0 + (port * 268435456))), 26880);
  }
}
void wall_oss_run_CONV3D_visual_patch_embed_projpatch_embed_bn_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/CONV3D_visual_patch_embed_projMVM_BN_write_to_HBM_bin/MVMCONV3D_HBM_DDR_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm1 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_0lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm2 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_0qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm3 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_0qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm4 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_0pos_emb_weight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 8; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/EMB_HBM_bin/EMBQ_pos_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm5 + (port * 268435456))), 65536);
  }
}
void wall_oss_run_blocks_0kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm6 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_0kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm7 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_0mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm8 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_0vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm9 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_0vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm10 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_0oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm11 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_0obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm12 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_0lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm13 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_0hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm14 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_0hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm15 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_0silu_weight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 1; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/ACT_HBM_bin/ACT_parameter_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm16 + (port * 268435456))), 128);
  }
}
void wall_oss_run_blocks_0hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm17 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_0hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm18 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_0hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm19 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_0hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_0/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm20 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_1lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm21 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_1qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm22 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_1qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm23 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_1kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm24 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_1kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm25 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_1mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm26 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_1vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm27 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_1vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm28 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_1oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm29 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_1obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm30 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_1lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm31 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_1hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm32 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_1hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm33 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_1hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm34 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_1hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm35 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_1hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm36 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_1hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_1/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm37 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_2lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm38 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_2qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm39 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_2qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm40 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_2kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm41 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_2kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm42 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_2mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm43 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_2vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm44 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_2vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm45 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_2oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm46 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_2obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm47 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_2lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm48 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_2hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm49 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_2hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm50 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_2hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm51 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_2hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm52 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_2hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm53 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_2hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_2/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm54 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_3lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm55 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_3qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm56 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_3qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm57 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_3kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm58 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_3kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm59 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_3mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm60 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_3vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm61 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_3vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm62 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_3oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm63 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_3obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm64 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_3lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm65 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_3hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm66 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_3hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm67 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_3hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm68 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_3hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm69 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_3hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm70 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_3hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_3/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm71 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks
+ (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_26lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm446 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_26qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm447 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_26qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm448 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_26kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm449 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_26kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm450 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_26mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm451 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_26vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm452 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_26vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm453 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_26oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm454 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_26obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm455 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_26lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm456 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_26hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm457 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_26hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm458 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_26hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm459 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_26hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm460 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_26hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm461 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_26hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_26/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm462 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_27lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm463 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_27qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm464 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_27qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm465 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_27kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm466 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_27kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm467 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_27mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm468 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_27vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm469 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_27vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm470 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_27oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm471 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_27obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm472 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_27lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm473 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_27hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm474 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_27hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm475 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_27hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm476 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_27hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm477 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_27hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm478 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_27hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_27/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm479 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_28lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm480 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_28qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm481 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_28qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm482 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_28kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm483 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_28kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm484 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_28mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm485 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_28vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm486 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_28vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm487 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_28oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm488 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_28obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm489 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_28lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm490 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_28hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm491 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_28hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm492 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_28hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm493 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_28hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm494 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_28hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm495 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_28hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_28/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm496 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_29lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm497 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_29qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm498 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_29qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm499 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_29kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm500 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_29kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm501 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_29mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm502 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_29vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm503 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_29vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm504 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_29oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm505 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_29obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm506 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_29lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm507 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_29hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm508 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_29hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm509 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_29hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm510 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_29hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm511 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_29hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm512 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_29hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_29/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm513 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_30lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm514 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_30qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm515 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_30qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm516 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_30kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm517 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_30kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm518 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_30mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm519 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_30vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm520 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_30vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm521 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_30oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm522 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_30obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm523 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_30lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm524 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_30hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm525 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_30hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm526 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_30hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm527 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_30hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm528 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_30hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm529 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_30hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_30/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm530 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_31lnweight0_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/LN_HBM_bin/LN0_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm531 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_31qweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_Q_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm532 + (port * 268435456))), 172032);
  }
}
void wall_oss_run_blocks_31qbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_Q_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm533 + (port * 268435456))), 1024);
  }
}
void wall_oss_run_blocks_31kweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_K_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm534 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_31kbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_K_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm535 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_31mask_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/ELEMENTWISE_HBM_bin/ATTEN_MASK_dat_in_B_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm536 + (port * 268435456))), 663552);
  }
}
void wall_oss_run_blocks_31vweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_V_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm537 + (port * 268435456))), 43008);
  }
}
void wall_oss_run_blocks_31vbias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN0_V_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm538 + (port * 268435456))), 256);
  }
}
void wall_oss_run_blocks_31oweight_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN1_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm539 + (port * 268435456))), 168960);
  }
}
void wall_oss_run_blocks_31obias_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN1_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm540 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_31lnweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/LN_HBM_bin/LN1_ln_wt_and_bias_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm541 + (port * 268435456))), 192);
  }
}
void wall_oss_run_blocks_31hweight1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN2_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm542 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_31hbias1_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN2_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm543 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_31hweight2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN3_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm544 + (port * 268435456))), 72576);
  }
}
void wall_oss_run_blocks_31hbias2_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN3_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm545 + (port * 268435456))), 448);
  }
}
void wall_oss_run_blocks_31hweight3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN4_wt_and_scale_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm546 + (port * 268435456))), 71680);
  }
}
void wall_oss_run_blocks_31hbias3_load_param(HANDLE& h2cx, char* prefix) {
  for (uint64_t port = 0; port < 32; port += 1) {
    char real_path[100];
    sprintf(real_path, "%s/wall_oss_run/blocks_31/MVM_BN_HBM_bin/MVMBN4_bn_wt_and_bias_in_HBM_%02llu.bin", prefix, port);
    DDR_Write_bin(h2cx, real_path, ((uint64_t)(hbm547 + (port * 268435456))), 192);
  }
}

void test_load_params(HANDLE& h2cx, char* prefix) {
  wall_oss_run_CONV3D_visual_patch_embed_projpatch_embed_wt_load_param(h2cx, prefix);
  wall_oss_run_CONV3D_visual_patch_embed_projpatch_embed_bn_load_param(h2cx, prefix);
  wall_oss_run_blocks_0lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_0qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_0pos_emb_weight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_0mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_0vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_0oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_0lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_0silu_weight_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_0hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_1lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_1qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_1qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_1kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_1kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_1mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_1vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_1vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_1oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_1obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_1lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_1hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_2lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_2qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_2qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_2kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_2kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_2mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_2vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_2vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_2oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_2obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_2lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_2hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_3lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_3qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_3qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_3kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_3kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_3mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_3vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_3vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_3oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_3obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_3lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_3hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_3hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_3hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_3hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks
weight_load_param(h2cx, prefix);
  wall_oss_run_blocks_26obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_26lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_26hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_27lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_27qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_27qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_27kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_27kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_27mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_27vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_27vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_27oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_27obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_27lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_27hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_28lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_28qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_28qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_28kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_28kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_28mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_28vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_28vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_28oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_28obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_28lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_28hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_29lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_29qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_29qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_29kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_29kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_29mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_29vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_29vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_29oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_29obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_29lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_29hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_30lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_30qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_30qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_30kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_30kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_30mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_30vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_30vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_30oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_30obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_30lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_30hbias3_load_param(h2cx, prefix);
  wall_oss_run_blocks_31lnweight0_load_param(h2cx, prefix);
  wall_oss_run_blocks_31qweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_31qbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_31kweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_31kbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_31mask_load_param(h2cx, prefix);
  wall_oss_run_blocks_31vweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_31vbias_load_param(h2cx, prefix);
  wall_oss_run_blocks_31oweight_load_param(h2cx, prefix);
  wall_oss_run_blocks_31obias_load_param(h2cx, prefix);
  wall_oss_run_blocks_31lnweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hweight1_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hbias1_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hweight2_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hbias2_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hweight3_load_param(h2cx, prefix);
  wall_oss_run_blocks_31hbias3_load_param(h2cx, prefix);
}

void mvm_f16xi4_step_1(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 105) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((860160 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm0 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm1 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1184);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_1\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm0 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm1 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1184);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_1\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void norm_step_2(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm2;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_2\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_3(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm3 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm4 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_3\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm3 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm4 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_3\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_4(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_4\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_5(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm6 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm7 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_5\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm6 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm7 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_5\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_6(HANDLE& device, int token, int last_token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * out_w_per_slice)) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 16);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 1028);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_6\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void kvcache2hbm_step_7(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache0;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 1);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_7\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_8(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * 8192) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 1024;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int out_ch_slice = ((1024 < (((((token + last_token) + 32) - 1) / 32) * 32)) ? 1024 : (((((token + last_token) + 32) - 1) / 32) * 32));
  int CHout_Split_Times = ((out_ch_slice < (((((token + last_token) + 32) - 1) / 32) * 32)) ? ((((((((token + last_token) + 32) - 1) / 32) * 32) + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = (((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) ? ((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = (((((8192 * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + 4) + 20);
  int total_clks_if_reuse_dat = ((((8192 * token) / 8192) + ((32768 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache0 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_8\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache0 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime2 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_8\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void elementwise_step_9(HANDLE& device) {
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 20736) * 32));
    uint64_t wt_base_addr = (hbm8 + ((w * 20736) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 20736) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, 663552);
    CSB_Write(device, 133, 663552);
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, 663552);
    CSB_Write(device, 136, 663552);
    CSB_Write(device, 137, 512);
    CSB_Write(device, 138, ((w < 0) ? 20736 : 20736));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, 663552);
    CSB_Write(device, 142, 663552);
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_9\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void softmax_step_10(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  if (((4096 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512)) > 8388608)) {
    Wout_Split_Times_minus1 = ((((token + (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1) / (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1);
    out_w_per_slice = (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16));
    out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * out_w_per_slice));
  }
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, (token + last_token));
    CSB_Write(device, 138, 16);
    CSB_Write(device, 139, (token + last_token));
    CSB_Write(device, 140, ((w < Wout_Split_Times_minus1) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 141, ((w * out_w_per_slice) + ((token + last_token) - token)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 8);
#ifdef PRINT_STEP
    printf("start softmax_step_10\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_11(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm9 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm10 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_11\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm9 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm10 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_11\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_12(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache1;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_12\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_13(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache1 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_13\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache1 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_13\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_14(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm11 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm12 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_14\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm11 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm12 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_14\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_15(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_15\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_16(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm13;
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_16\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_17(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm14 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm15 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_17\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm14 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm15 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_17\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_18(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_18\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_19(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm17 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm18 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_19\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm17 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm18 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_19\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_20(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime3 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_20\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_21(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm19 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm20 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_21\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime1 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm19 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm20 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_21\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_22(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_22\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_23(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm21;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_23\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_24(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm22 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm23 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_24\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm22 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm23 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_24\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_25(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_25\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_26(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm24 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm25 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_26\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm24 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm25 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_26\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_27(HANDLE& device, int token, int last_token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * out_w_per_slice)) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 16);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 1028);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_27\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void kvcache2hbm_step_28(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime3;
  uint64_t feature_out_addr = hbm_cache2;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 1);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_28\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_29(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * 8192) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 1024;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int out_ch_slice = ((1024 < (((((token + last_token) + 32) - 1) / 32) * 32)) ? 1024 : (((((token + last_token) + 32) - 1) / 32) * 32));
  int CHout_Split_Times = ((out_ch_slice < (((((token + last_token) + 32) - 1) / 32) * 32)) ? ((((((((token + last_token) + 32) - 1) / 32) * 32) + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = (((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) ? ((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = (((((8192 * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + 4) + 20);
  int total_clks_if_reuse_dat = ((((8192 * token) / 8192) + ((32768 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache2 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_29\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache2 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_29\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void elementwise_step_30(HANDLE& device) {
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 20736) * 32));
    uint64_t wt_base_addr = (hbm26 + ((w * 20736) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 20736) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, 663552);
    CSB_Write(device, 133, 663552);
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, 663552);
    CSB_Write(device, 136, 663552);
    CSB_Write(device, 137, 512);
    CSB_Write(device, 138, ((w < 0) ? 20736 : 20736));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, 663552);
    CSB_Write(device, 142, 663552);
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_30\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void softmax_step_31(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  if (((4096 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512)) > 8388608)) {
    Wout_Split_Times_minus1 = ((((token + (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1) / (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1);
    out_w_per_slice = (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16));
    out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * out_w_per_slice));
  }
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, (token + last_token));
    CSB_Write(device, 138, 16);
    CSB_Write(device, 139, (token + last_token));
    CSB_Write(device, 140, ((w < Wout_Split_Times_minus1) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 141, ((w * out_w_per_slice) + ((token + last_token) - token)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 8);
#ifdef PRINT_STEP
    printf("start softmax_step_31\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_32(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm27 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm28 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_32\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm27 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm28 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_32\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_33(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache3;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_33\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_34(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache3 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_34\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache3 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_34\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_35(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm29 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm30 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_35\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm29 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm30 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_35\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_36(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_36\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_37(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm31;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_37\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_38(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm32 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm33 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_38\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm32 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm33 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_38\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_39(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_39\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_40(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm34 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm35 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_40\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm34 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm35 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_40\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_41(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_41\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_42(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm36 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm37 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_42\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm36 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm37 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_42\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_43(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_43\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_44(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm38;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_44\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_45(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm39 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm40 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_45\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm39 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm40 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_45\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_46(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_46\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_47(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm41 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hb
+ (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_599\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm486 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm487 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_599\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_600(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache57;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_600\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_601(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache57 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_601\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache57 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_601\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_602(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm488 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm489 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_602\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm488 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm489 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_602\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_603(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_603\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_604(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm490;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_604\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_605(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm491 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm492 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_605\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm491 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm492 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_605\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_606(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_606\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_607(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm493 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm494 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_607\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm493 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm494 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_607\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_608(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_608\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_609(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm495 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm496 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_609\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm495 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm496 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_609\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_610(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_610\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_611(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm497;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_611\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_612(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm498 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm499 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_612\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm498 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm499 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_612\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_613(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_613\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_614(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm500 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm501 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_614\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm500 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm501 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_614\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_615(HANDLE& device, int token, int last_token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * out_w_per_slice)) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 16);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 1028);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_615\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void kvcache2hbm_step_616(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime3;
  uint64_t feature_out_addr = hbm_cache58;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 1);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_616\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_617(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * 8192) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 1024;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int out_ch_slice = ((1024 < (((((token + last_token) + 32) - 1) / 32) * 32)) ? 1024 : (((((token + last_token) + 32) - 1) / 32) * 32));
  int CHout_Split_Times = ((out_ch_slice < (((((token + last_token) + 32) - 1) / 32) * 32)) ? ((((((((token + last_token) + 32) - 1) / 32) * 32) + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = (((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) ? ((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = (((((8192 * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + 4) + 20);
  int total_clks_if_reuse_dat = ((((8192 * token) / 8192) + ((32768 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache58 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_617\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache58 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_617\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void elementwise_step_618(HANDLE& device) {
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 20736) * 32));
    uint64_t wt_base_addr = (hbm502 + ((w * 20736) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 20736) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, 663552);
    CSB_Write(device, 133, 663552);
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, 663552);
    CSB_Write(device, 136, 663552);
    CSB_Write(device, 137, 512);
    CSB_Write(device, 138, ((w < 0) ? 20736 : 20736));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, 663552);
    CSB_Write(device, 142, 663552);
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_618\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void softmax_step_619(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  if (((4096 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512)) > 8388608)) {
    Wout_Split_Times_minus1 = ((((token + (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1) / (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1);
    out_w_per_slice = (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16));
    out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * out_w_per_slice));
  }
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, (token + last_token));
    CSB_Write(device, 138, 16);
    CSB_Write(device, 139, (token + last_token));
    CSB_Write(device, 140, ((w < Wout_Split_Times_minus1) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 141, ((w * out_w_per_slice) + ((token + last_token) - token)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 8);
#ifdef PRINT_STEP
    printf("start softmax_step_619\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_620(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm503 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm504 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_620\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm503 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm504 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_620\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_621(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache59;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_621\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_622(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache59 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_622\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache59 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_622\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_623(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm505 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm506 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_623\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm505 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm506 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_623\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_624(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_624\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_625(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm507;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_625\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_626(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm508 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm509 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_626\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm508 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm509 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_626\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_627(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_627\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_628(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm510 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm511 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_628\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm510 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm511 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_628\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_629(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_629\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_630(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm512 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm513 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_630\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm512 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm513 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_630\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_631(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_631\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_632(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm514;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_632\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_633(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm515 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm516 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_633\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm515 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm516 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_633\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_634(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_634\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_635(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm517 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm518 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_635\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm517 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm518 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_635\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_636(HANDLE& device, int token, int last_token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * out_w_per_slice)) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 16);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 1028);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_636\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void kvcache2hbm_step_637(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime3;
  uint64_t feature_out_addr = hbm_cache60;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 1);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_637\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_638(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * 8192) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 1024;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int out_ch_slice = ((1024 < (((((token + last_token) + 32) - 1) / 32) * 32)) ? 1024 : (((((token + last_token) + 32) - 1) / 32) * 32));
  int CHout_Split_Times = ((out_ch_slice < (((((token + last_token) + 32) - 1) / 32) * 32)) ? ((((((((token + last_token) + 32) - 1) / 32) * 32) + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = (((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) ? ((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = (((((8192 * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + 4) + 20);
  int total_clks_if_reuse_dat = ((((8192 * token) / 8192) + ((32768 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache60 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_638\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache60 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_638\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void elementwise_step_639(HANDLE& device) {
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 20736) * 32));
    uint64_t wt_base_addr = (hbm519 + ((w * 20736) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 20736) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, 663552);
    CSB_Write(device, 133, 663552);
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, 663552);
    CSB_Write(device, 136, 663552);
    CSB_Write(device, 137, 512);
    CSB_Write(device, 138, ((w < 0) ? 20736 : 20736));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, 663552);
    CSB_Write(device, 142, 663552);
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_639\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void softmax_step_640(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  if (((4096 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512)) > 8388608)) {
    Wout_Split_Times_minus1 = ((((token + (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1) / (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1);
    out_w_per_slice = (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16));
    out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * out_w_per_slice));
  }
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, (token + last_token));
    CSB_Write(device, 138, 16);
    CSB_Write(device, 139, (token + last_token));
    CSB_Write(device, 140, ((w < Wout_Split_Times_minus1) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 141, ((w * out_w_per_slice) + ((token + last_token) - token)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 8);
#ifdef PRINT_STEP
    printf("start softmax_step_640\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_641(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm520 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm521 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_641\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm520 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm521 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_641\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_642(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache61;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_642\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_643(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache61 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_643\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache61 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_643\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_644(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm522 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm523 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_644\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm522 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm523 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_644\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_645(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_645\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_646(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm524;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_646\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_647(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm525 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm526 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_647\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm525 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm526 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_647\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_648(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_648\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_649(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm527 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm528 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_649\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm527 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm528 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_649\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_650(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_650\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_651(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm529 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm530 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_651\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm529 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm530 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_651\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_652(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_652\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_653(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm531;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_653\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_654(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 8) * 16) / 8192) + 672) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((5505024 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 8; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm532 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm533 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_654\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 8; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm532 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm533 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 7) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_654\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_655(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 64) - 1) / 64) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 64));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 64) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * 64)) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 64) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 64 : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 64);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 4100);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_655\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_656(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm534 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm535 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_656\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm534 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm535 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_656\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void rope_step_657(HANDLE& device, int token, int last_token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (hbm5 + ((last_token + (w * out_w_per_slice)) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 128);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, 0);
    CSB_Write(device, 140, 16);
    CSB_Write(device, 141, 65536);
    CSB_Write(device, 142, 65536);
    CSB_Write(device, 143, 1028);
    CSB_Write(device, 144, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 145, 4);
#ifdef PRINT_STEP
    printf("start rope_step_657\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void kvcache2hbm_step_658(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime3;
  uint64_t feature_out_addr = hbm_cache62;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 1);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_658\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_659(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * 8192) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 1024;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int out_ch_slice = ((1024 < (((((token + last_token) + 32) - 1) / 32) * 32)) ? 1024 : (((((token + last_token) + 32) - 1) / 32) * 32));
  int CHout_Split_Times = ((out_ch_slice < (((((token + last_token) + 32) - 1) / 32) * 32)) ? ((((((((token + last_token) + 32) - 1) / 32) * 32) + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = (((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) ? ((((((token + last_token) + 32) - 1) / 32) * 32) % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = (((((8192 * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + 4) + 20);
  int total_clks_if_reuse_dat = ((((8192 * token) / 8192) + ((32768 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache62 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_659\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime2 + (32 * (w * out_w_slice))) + (h * (32 * token)));
          uint64_t wt_base_addr = ((hbm_cache62 + (((256 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = (((runtime3 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * out_ch_slice) / 512))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          CSB_Write(device, 2, 128);
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 4);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 0);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, 2048);
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 3);
          CSB_Write(device, 18, 227646);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 0);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 1);
          CSB_Write(device, 29, (32 * token));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_659\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void elementwise_step_660(HANDLE& device) {
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime3 + ((w * 20736) * 32));
    uint64_t wt_base_addr = (hbm536 + ((w * 20736) * 32));
    uint64_t feature_out_addr = (runtime2 + ((w * 20736) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, 663552);
    CSB_Write(device, 133, 663552);
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, 663552);
    CSB_Write(device, 136, 663552);
    CSB_Write(device, 137, 512);
    CSB_Write(device, 138, ((w < 0) ? 20736 : 20736));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, 663552);
    CSB_Write(device, 142, 663552);
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_660\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void softmax_step_661(HANDLE& device, int token, int last_token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  if (((4096 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512)) > 8388608)) {
    Wout_Split_Times_minus1 = ((((token + (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1) / (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16))) - 1);
    out_w_per_slice = (8388608 / ((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16));
    out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * out_w_per_slice));
  }
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, (token + last_token));
    CSB_Write(device, 138, 16);
    CSB_Write(device, 139, (token + last_token));
    CSB_Write(device, 140, ((w < Wout_Split_Times_minus1) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 141, ((w * out_w_per_slice) + ((token + last_token) - token)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 8);
#ifdef PRINT_STEP
    printf("start softmax_step_661\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_662(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 2) * 16) / 8192) + 168) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((1376256 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm537 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm538 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_662\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm537 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime4 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm538 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 1024));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_662\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void kvcache2hbm_step_663(HANDLE& device, int token, int last_token) {
  uint64_t feature_in_addr = runtime4;
  uint64_t feature_out_addr = hbm_cache63;
  CSB_Write(device, 130, (((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)));
  CSB_Write(device, 131, ((uint32_t)feature_in_addr));
  CSB_Write(device, 132, (32 * token));
  CSB_Write(device, 133, (32 * token));
  CSB_Write(device, 134, ((uint32_t)feature_out_addr));
  CSB_Write(device, 135, 16384);
  CSB_Write(device, 136, 4096);
  CSB_Write(device, 137, 128);
  CSB_Write(device, 138, 1);
  CSB_Write(device, 139, token);
  CSB_Write(device, 140, 16);
  CSB_Write(device, 141, last_token);
  CSB_Write(device, 142, 2);
  CSB_Write(device, 143, 1028);
  CSB_Write(device, 144, 0);
  CSB_Write(device, 145, 64);
#ifdef PRINT_STEP
  printf("start kvcache2hbm_step_663\n");
#endif
  while (CSB_Read(device, 129) != 1) {}
}
void mvm_f16xf16_step_664(HANDLE& device, int token, int last_token) {
  int min_dat_depth = ((token * (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16)) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = (8388608 / (((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 512) * 16) * 16));
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int tp_log_out_ch_slice = (1 << ((int)(log2(((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)))));
  int tp_out_ch_slice = ((tp_log_out_ch_slice > ((16777216 / ((32 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16)) * 32)) ? (tp_log_out_ch_slice / 2) : tp_log_out_ch_slice);
  int out_ch_slice = ((((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) < 128) ? ((tp_out_ch_slice > 1024) ? 1024 : tp_out_ch_slice) : 128);
  int CHout_Split_Times = ((out_ch_slice < 128) ? (((128 + out_ch_slice) - 1) / out_ch_slice) : 1);
  int out_ch_slice_last = ((128 % out_ch_slice) ? (128 % out_ch_slice) : out_ch_slice);
  int total_clks_if_reuse_wt = ((((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) * ((CHout_Split_Times - 1) + 1)) / 8192) + ((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 8192)) + 20);
  int total_clks_if_reuse_dat = (((((((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512) * 8192) * token) / 8192) + (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int h = 0; h < 16; h += 1) {
      for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
        for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache63 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, (w ? 1 : 0));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_664\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  } else {
    for (int h = 0; h < 16; h += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        for (int ch = 0; ch < ((CHout_Split_Times - 1) + 1); ch += 1) {
          uint64_t feature_in_addr = ((runtime3 + (32 * (w * out_w_slice))) + (h * ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512))));
          uint64_t wt_base_addr = ((hbm_cache63 + (((4096 * ch) * out_ch_slice) / 32)) + (h * 16384));
          uint64_t feature_out_addr = ((runtime2 + (32 * (w * out_w_slice))) + ((h * (32 * token)) / 1));
          CSB_Write(device, 2, (((((token + last_token) + 32) - 1) / 32) * 32));
          CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 4, 1);
          CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
          CSB_Write(device, 6, 1);
          CSB_Write(device, 7, ((ch < (CHout_Split_Times - 1)) ? out_ch_slice : out_ch_slice_last));
          CSB_Write(device, 8, (ch * out_ch_slice));
          CSB_Write(device, 9, 128);
          CSB_Write(device, 10, ((uint32_t)feature_in_addr));
          CSB_Write(device, 11, ((uint32_t)wt_base_addr));
          CSB_Write(device, 12, (((((128 * (((((((((token + last_token) + 32) - 1) / 32) * 32) + 32) - 1) / 32) * 32)) * 16) / 8) / 128) * 8));
          CSB_Write(device, 13, ((uint32_t)feature_out_addr));
          CSB_Write(device, 14, 0);
          CSB_Write(device, 15, 0);
          CSB_Write(device, 16, ((ch ? 1 : 0) * 2));
          CSB_Write(device, 17, 4);
          CSB_Write(device, 18, 224577);
          CSB_Write(device, 19, 65792);
          CSB_Write(device, 20, 65792);
          CSB_Write(device, 21, 0);
          CSB_Write(device, 22, 0);
          CSB_Write(device, 23, 0);
          CSB_Write(device, 24, 0);
          CSB_Write(device, 25, 0);
          CSB_Write(device, 26, 4096);
          CSB_Write(device, 27, 260);
          CSB_Write(device, 28, 2);
          CSB_Write(device, 29, ((32 * token) * ((((((((token + last_token) + 32) - 1) / 32) * 32) + 512) - 1) / 512)));
          CSB_Write(device, 30, (32 * token));
          CSB_Write(device, 31, (32 * token));
          CSB_Write(device, 32, (32 * token));
          CSB_Write(device, 33, ((uint32_t)((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16))));
          CSB_Write(device, 34, 15);
#ifdef PRINT_STEP
          printf("start mvm_f16xf16_step_664\n");
#endif
          while (CSB_Read(device, 1) != 1) {}
        }
      }
    }
  }
}
void mvm_f16xi4_step_665(HANDLE& device, int token) {
  int min_dat_depth = (((token * 8192) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 64;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((8192 * token) * 3) * 16) / 8192) + 660) + 20);
  int total_clks_if_reuse_dat = (((((8192 * token) * 16) / 8192) + ((5406720 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 3; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm539 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm540 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_665\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 3; ch += 1) {
        uint64_t feature_in_addr = (runtime2 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm539 + (67584 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 512) / 512)));
        uint64_t BN_base_addr = (hbm540 + (32 * ((ch * 512) / 256)));
        CSB_Write(device, 2, 8192);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 2) ? 512 : 256));
        CSB_Write(device, 8, (ch * 512));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 33792);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 2048);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_665\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_666(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_666\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void norm_step_667(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = hbm541;
    uint64_t feature_out_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, ((32 * token) * 3));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, ((32 * token) * 3));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 140, 0);
    CSB_Write(device, 141, 168755);
    CSB_Write(device, 142, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 143, 66305);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 32);
#ifdef PRINT_STEP
    printf("start norm_step_667\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_668(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm542 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm543 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_668\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm542 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm543 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_668\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void activate_step_669(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime1 + ((w * 146) * 32));
    uint64_t wt_base_addr = hbm16;
    uint64_t feature_out_addr = (runtime2 + ((w * 146) * 32));
    CSB_Write(device, 130, ((uint32_t)wt_base_addr));
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, 1);
    CSB_Write(device, 139, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 140, (w ? 2 : 1));
    CSB_Write(device, 141, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 142, 0);
    CSB_Write(device, 143, 0);
    CSB_Write(device, 144, 0);
    CSB_Write(device, 145, 16);
#ifdef PRINT_STEP
    printf("start activate_step_669\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_670(HANDLE& device, int token) {
  int min_dat_depth = (((token * 1536) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 341;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((1536 * token) * 4) * 16) / 8192) + 283) + 20);
  int total_clks_if_reuse_dat = (((((1536 * token) * 16) / 8192) + ((2322432 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 4; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm544 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm545 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_670\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 4; ch += 1) {
        uint64_t feature_in_addr = (runtime0 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm544 + (21504 * ch));
        uint64_t feature_out_addr = ((runtime1 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm545 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 1280);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 3) ? 1024 : 384));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 5376);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1280);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_670\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_671(HANDLE& device, int token) {
  int Wout_Split_Times_minus1 = 0;
  int out_w_in_last_slice = token;
  Wout_Split_Times_minus1 = ((((token + 146) - 1) / 146) - 1);
  out_w_in_last_slice = (token - (Wout_Split_Times_minus1 * 146));
  for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
    uint64_t feature_in_addr = (runtime2 + ((w * 146) * 32));
    uint64_t wt_base_addr = (runtime1 + ((w * 146) * 32));
    uint64_t feature_out_addr = (runtime3 + ((w * 146) * 32));
    CSB_Write(device, 130, 2);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 3456);
    CSB_Write(device, 138, ((w < Wout_Split_Times_minus1) ? 146 : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_671\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}
void mvm_f16xi4_step_672(HANDLE& device, int token) {
  int min_dat_depth = (((token * 3584) * 16) / 8192);
  int Wout_Split_Times_minus1 = 0;
  int out_w_slice = token;
  int out_w_slice_last = token;
  if ((min_dat_depth > 1024)) {
    out_w_slice = 146;
    out_w_slice_last = ((token % out_w_slice) ? (token % out_w_slice) : out_w_slice);
    Wout_Split_Times_minus1 = ((((token + out_w_slice) - 1) / out_w_slice) - 1);
  }
  int total_clks_if_reuse_wt = ((((((3584 * token) * 2) * 16) / 8192) + 280) + 20);
  int total_clks_if_reuse_dat = (((((3584 * token) * 16) / 8192) + ((2293760 * (Wout_Split_Times_minus1 + 1)) / 8192)) + 20);
  if ((total_clks_if_reuse_wt < total_clks_if_reuse_dat)) {
    for (int ch = 0; ch < 2; ch += 1) {
      for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm546 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm547 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, ((w ? 1 : 0) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_672\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  } else {
    for (int w = 0; w < (Wout_Split_Times_minus1 + 1); w += 1) {
      for (int ch = 0; ch < 2; ch += 1) {
        uint64_t feature_in_addr = (runtime3 + (32 * (w * out_w_slice)));
        uint64_t wt_base_addr = (hbm546 + (57344 * ch));
        uint64_t feature_out_addr = ((runtime0 + (32 * (w * out_w_slice))) + ((32 * token) * ((ch * 1024) / 512)));
        uint64_t BN_base_addr = (hbm547 + (32 * ((ch * 1024) / 256)));
        CSB_Write(device, 2, 3456);
        CSB_Write(device, 3, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 4, 1);
        CSB_Write(device, 5, ((w < Wout_Split_Times_minus1) ? out_w_slice : out_w_slice_last));
        CSB_Write(device, 6, 1);
        CSB_Write(device, 7, ((ch < 1) ? 1024 : 256));
        CSB_Write(device, 8, (ch * 1024));
        CSB_Write(device, 9, 0);
        CSB_Write(device, 10, ((uint32_t)feature_in_addr));
        CSB_Write(device, 11, ((uint32_t)wt_base_addr));
        CSB_Write(device, 12, 14336);
        CSB_Write(device, 13, ((uint32_t)feature_out_addr));
        CSB_Write(device, 14, 0);
        CSB_Write(device, 15, 0);
        CSB_Write(device, 16, (((ch ? 1 : 0) * 2) + 4));
        CSB_Write(device, 17, 1);
        CSB_Write(device, 18, 0);
        CSB_Write(device, 19, 65792);
        CSB_Write(device, 20, 65792);
        CSB_Write(device, 21, 0);
        CSB_Write(device, 22, 11536384);
        CSB_Write(device, 23, 7340160);
        CSB_Write(device, 24, 1408);
        CSB_Write(device, 25, ((uint32_t)BN_base_addr));
        CSB_Write(device, 26, 0);
        CSB_Write(device, 27, 0);
        CSB_Write(device, 28, 0);
        CSB_Write(device, 29, (32 * token));
        CSB_Write(device, 30, (32 * token));
        CSB_Write(device, 31, (32 * token));
        CSB_Write(device, 32, (32 * token));
        CSB_Write(device, 33, ((uint32_t)(((BN_base_addr & 1095216660480) >> 8) + ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)))));
        CSB_Write(device, 34, 31);
#ifdef PRINT_STEP
        printf("start mvm_f16xi4_step_672\n");
#endif
        while (CSB_Read(device, 1) != 1) {}
      }
    }
  }
}
void elementwise_step_673(HANDLE& device, int token) {
  int out_w_per_slice = token;
  int out_w_in_last_slice = token;
  for (int w = 0; w < 1; w += 1) {
    uint64_t feature_in_addr = (runtime0 + ((w * out_w_per_slice) * 32));
    uint64_t wt_base_addr = (runtime4 + ((w * out_w_per_slice) * 32));
    uint64_t feature_out_addr = (runtime1 + ((w * out_w_per_slice) * 32));
    CSB_Write(device, 130, 0);
    CSB_Write(device, 131, ((uint32_t)feature_in_addr));
    CSB_Write(device, 132, (32 * token));
    CSB_Write(device, 133, (32 * token));
    CSB_Write(device, 134, ((uint32_t)feature_out_addr));
    CSB_Write(device, 135, (32 * token));
    CSB_Write(device, 136, (32 * token));
    CSB_Write(device, 137, 1280);
    CSB_Write(device, 138, ((w < 0) ? out_w_per_slice : out_w_in_last_slice));
    CSB_Write(device, 139, ((((feature_in_addr & 1095216660480) >> 32) + ((feature_out_addr & 1095216660480) >> 24)) + ((wt_base_addr & 1095216660480) >> 16)));
    CSB_Write(device, 140, ((uint32_t)wt_base_addr));
    CSB_Write(device, 141, (32 * token));
    CSB_Write(device, 142, (32 * token));
    CSB_Write(device, 145, 2);
#ifdef PRINT_STEP
    printf("start elementwise_step_673\n");
#endif
    while (CSB_Read(device, 129) != 1) {}
  }
}

void test(HANDLE& device, int token, int last_token) {
  mvm_f16xi4_step_1(device, token);
  norm_step_2(device, token);
  mvm_f16xi4_step_3(device, token);
  rope_step_4(device, token, last_token);
  mvm_f16xi4_step_5(device, token);
  rope_step_6(device, token, last_token);
  kvcache2hbm_step_7(device, token, last_token);
  mvm_f16xf16_step_8(device, token, last_token);
  elementwise_step_9(device);
  softmax_step_10(device, token, last_token);
  mvm_f16xi4_step_11(device, token);
  kvcache2hbm_step_12(device, token, last_token);
  mvm_f16xf16_step_13(device, token, last_token);
  mvm_f16xi4_step_14(device, token);
  elementwise_step_15(device, token);
  norm_step_16(device, token);
  mvm_f16xi4_step_17(device, token);
  activate_step_18(device, token);
  mvm_f16xi4_step_19(device, token);
  elementwise_step_20(device, token);
  mvm_f16xi4_step_21(device, token);
  elementwise_step_22(device, token);
  norm_step_23(device, token);
  mvm_f16xi4_step_24(device, token);
  rope_step_25(device, token, last_token);
  mvm_f16xi4_step_26(device, token);
  rope_step_27(device, token, last_token);
  kvcache2hbm_step_28(device, token, last_token);
  mvm_f16xf16_step_29(device, token, last_token);
  elementwise_step_30(device);
  softmax_step_31(device, token, last_token);
  mvm_f16xi4_step_32(device, token);
  kvcache2hbm_step_33(device, token, last_token);
  mvm_f16xf16_step_34(device, token, last_token);
  mvm_f16xi4_step_35(device, token);
  elementwise_step_36(device, token);
  norm_step_37(device, token);
  mvm_f16xi4_step_38(device, token);
  activate_step_39(device, token);
  mvm_f16xi4_step_40(device, token);
  elementwise_step_41(device, token);
  mvm_f16xi4_step_42(device, token);
  elementwise_step_43(device, token);
  norm_step_44(device, token);
  mvm_f16xi4_step_45(device, token);
  rope_step_46(device, token, last_token);
  mvm_f16xi4_step_47(device, token);
  rope_step_48(device, token, last_token);
  kvcache2hbm_step_49(device, token, last_token);
  mvm_f16xf16_step_50(device, token, last_token);
  elementwise_step_51(device);
  softmax_step_52(device, token, last_token);
  mvm_f16xi4_step_53(device, token);
  kvcache2hbm_step_54(device, token, last_token);
  mvm_f16xf16_step_55(device, token, last_token);
  mvm_f16xi4_step_56(device, token);
  elementwise_step_57(device, token);
  norm_step_58(device, token);
  mvm_f16xi4_step_59(device, token);
  activate_step_60(device, token);
  mvm_f16xi4_step_61(device, token);
  elementwise_step_62(device, token);
  mvm_f16xi4_step_63(device, token);
  elementwise_step_64(device, token);
  norm_step_65(device, token);
  mvm_f16xi4_step_66(device, token);
  rope_step_67(device, token, last_token);
  mvm_f16xi4_step_68(device, token);
  rope_step_69(device, token, last_token);
  kvcache2hbm_step_70(device, token, last_token);
  mvm_f16xf16_step_71(device, token, last_token);
  elementwise_step_72(device);
  softmax_step_73(device, token, last_token);
  mvm_f16xi4_step_74(device, token);
  kvcache2hbm_step_75(device, token, last_token);
  mvm_f16xf16_step_76(device, token, last_token);
  mvm_f16xi4_step_77(device, token);
  elementwise_step_78(device, token);
  norm_step_79(device, token);
  mvm_f16xi4_step_80(device, token);
  activate_step_81(device, token);
  mvm_f16xi4_step_82(device, token);
  elementwise_step_83(device, token);
  mvm_f16xi4_step_84(device, token);
  elementwise_step_85(device, token);
  norm_step_86(device, token);
  mvm_f16xi4_step_87(device, token);
  rope_step_88(device, token, last_token);
  mvm_f16xi4_step_89(device, token);
  rope_step_90(device, token, last_token);
  kvcache2hbm_step_91(device, token, last_token);
  mvm_f16xf16_step_92(device, token, last_token);
  elementwise_step_93(device);
  softmax_step_94(device, token, last_token);
  mvm_f16xi4_step_95(device, token);
  kvcache2hbm_step_96(device, token, last_token);
  mvm_f16xf16_step_97(device, token, last_token);
  mvm_f16xi4_step_98(device, token);
  elementwise_step_99(device, token);
  norm_step_100(device, token);
  mvm_f16xi4_step_101(device, token);
  activate_step_102(device, token);
  mvm_f16xi4_step_103(device, token);
  elementwise_step_104(device, token);
  mvm_f16xi4_step_105(device, token);
  elementwise_step_106(device, token);
  norm_step_107(device, token);
  mvm_f16xi4_step_108(device, token);
  rope_step_109(device, token, last_token);
  mvm_f16xi4_step_110(device, token);
  rope_step_111(device, token, last_token);
  kvcache2hbm_step_112(device, token, last_token);
  mvm_f16xf16_step_113(device, token, last_token);
  elementwise_step_114(device);
  softmax_step_115(device, token, last_token);
  mvm_f16xi4_step_116(device, token);
  kvcache2hbm_step_117(device, token, last_token);
  mvm_f16xf16_step_118(device, token, last_token);
  mvm_f16xi4_step_119(device, token);
  elementwise_step_120(device, token);
  norm_step_121(device, token);
  mvm_f16xi4_step_122(device, token);
  activate_step_123(device, token);
  mvm_f16xi4_step_124(device, token);
  elementwise_step_125(device, token);
  mvm_f16xi4_step_126(device, token);
  elementwise_step_127(device, token);
  norm_step_128(device, token);
  mvm_f16xi4_step_129(device, token);
  rope_step_130(device, token, last_token);
  mvm_f16xi4_step_131(device, token);
  rope_step_132(device, token, last_token);
  kvcache2hbm_step_133(device, token, last_token);
  mvm_f16xf16_step_134(device, token, last_token);
  elementwise_step_135(device);
  softmax_step_136(device, token, last_token);
  mvm_f16xi4_step_137(device, token);
  kvcache2hbm_step_138(device, token, last_token);
  mvm_f16xf16_step_139(device, token, last_token);
  mvm_f16xi4_step_140(device, token);
  elementwise_step_141(device, token);
  norm_step_142(device, token);
  mvm_f16xi4_step_143(device, token);
  activate_step_144(device, token);
  mvm_f16xi4_step_145(device, token);
  elementwise_step_146(device, token);
  mvm_f16xi4_step_147(device, token);
  elementwise_step_148(device, token);
  norm_step_149(device, token);
  mvm_f16xi4_step_150(device, token);
  rope_step_151(device, token, last_token);
  mvm_f16xi4_step_152(device, token);
  rope_step_153(device, token, last_token);
  kvcache2hbm_step_154(device, token, last_token);
  mvm_f16xf16_step_155(device, token, last_token);
  elementwise_step_156(device);
  softmax_step_157(device, token, last_token);
  mvm_f16xi4_step_158(device, token);
  kvcache2hbm_step_159(device, token, last_token);
  mvm_f16xf16_step_160(device, token, last_token);
  mvm_f16xi4_step_161(device, token);
  elementwise_step_162(device, token);
  norm_step_163(device, token);
  mvm_f16xi4_step_164(device, token);
  activate_step_165(device, token);
  mvm_f16xi4_step_166(device, token);
  elementwise_step_167(device, token);
  mvm_f16xi4_step_168(device, token);
  elementwise_step_169(device, token);
  norm_step_170(device, token);
  mvm_f16xi4_step_171(device, token);
  rope_step_172(device, token, last_token);
  mvm_f16xi4_step_173(device, token);
  rope_step_174(device, token, last_token);
  kvcache2hbm_step_175(device, token, last_token);
  mvm_f16xf16_step_176(device, token, last_token);
  elementwise_step_177(device);
  softmax_step_178(device, token, last_token);
  mvm_f16xi4_step_179(device, token);
  kvcache2hbm_step_180(device, token, last_token);
  mvm_f16xf16_step_181(device, token, last_token);
  mvm_f16xi4_step_182(device, token);
  elementwise_step_183(device, token);
  norm_step_184(device, token);
  mvm_f16xi4_step_185(device, token);
  activate_step_186(device, token);
  mvm_f16xi4_step_187(device, token);
  elementwise_step_188(device, token);
  mvm_f16xi4_step_189(device, token);
  elementwise_step_190(device, token);
  norm_step_191(device, token);
  mvm_f16xi4_step_192(device, token);
  rope_step_193(device, token, last_token);
  mvm_f16xi4_step_194(device, token);
  rope_step_195(device, token, last_token);
  kvcache2hbm_step_196(device, token, last_token);
  mvm_f16xf16_step_197(device, token, last_token);
  elementwise_step_198(device);
  softmax_step_199(device, token, last_token);
  mvm_f16xi4_step_200(device, token);
  kvcache2hbm_step_201(device, token, last_token);
  mvm_f16xf16_step_202(device, token, last_token);
  mvm_f16xi4_step_203(device, token);
  elementwise_step_204(device, token);
  norm_step_205(device, token);
  mvm_f16xi4_step_206(device, token);
  activate_step_207(device, token);
  mvm_f16xi4_step_208(device, token);
  elementwise_step_209(device, token);
  mvm_f16xi4_step_210(device, token);
  elementwise_step_211(device, token);
  norm_step_212(device, token);
  mvm_f16xi4_step_213(device, token);
  rope_step_214(device, token, last_token);
  mvm_f16xi4_step_215(device, token);
  rope_step_216(device, token, last_token);
  kvcache2hbm_step_217(device, token, last_token);
  mvm_f16xf16_step_218(device, token, last_token);
  elementwise_step_219(device);
  softmax_step_220(device, token, last_token);
  mvm_f16xi4_step_221(device, token);
  kvcache2hbm_step_222(device, token, last_token);
  mvm_f16xf16_step_223(device, token, last_token);
  mvm_f16xi4_step_224(device, token);
  elementwise_step_225(device, token);
  norm_step_226(device, token);
  mvm_f16xi4_step_227(device, token);
  activate_step_228(device, token);
  mvm_f16xi4_step_229(device, token);
  elementwise_step_230(device, token);
  mvm_f16xi4_step_231(device, token);
  elementwise_step_232(device, token);
  norm_step_233(device, token);
  mvm_f16xi4_step_234(device, token);
  rope_step_235(device, token, last_token);
  mvm_f16xi4_step_236(device, token);
  rope_step_237(device, token, last_token);
  kvcache2hbm_step_238(device, token, last_token);
  mvm_f16xf16_step_239(device, token, last_token);
  elementwise_step_240(device);
  softmax_step_241(device, token, last_token);
  mvm_f16xi4_step_242(device, token);
  kvcache2hbm_step_243(device, token, last_token);
  mvm_f16xf16_step_244(device, token, last_token);
  mvm_f16xi4_step_245(device, token);
  elementwise_step_246(device, token);
  norm_step_247(device, token);
  mvm_f16xi4_step_248(device, token);
  activate_step_249(device, token);
  mvm_f16xi4_step_250(device, token);
  elementwise_step_251(device, token);
  mvm_f16xi4_step_252(device, token);
  elementwise_step_253(device, token);
  norm_step_254(device, token);
  mvm_f16xi4_step_255(device, token);
  rope_step_256(device, token, last_token);
  mvm_f16xi4_step_257(device, token);
  rope_step_258(device, token, last_token);
  kvcache2hbm_step_259(device, token, last_token);
  mvm_f16xf16_step_260(device, token, last_token);
  elementwise_step_261(device);
  softmax_step_262(device, token, last_token);
  mvm_f16xi4_step_263(device, token);
  kvcache2hbm_step_264(device, token, last_token);
  mvm_f16xf16_step_265(device, token, last_token);
  mvm_f16xi4_step_266(device, token);
  elementwise_step_267(device, token);
  norm_step_268(device, token);
  mvm_f16xi4_step_269(device, token);
  activate_step_270(device, token);
  mvm_f16xi4_step_271(device, token);
  elementwise_step_272(device, token);
  mvm_f16xi4_step_273(device, token);
  elementwise_step_274(device, token);
  norm_step_275(device, token);
  mvm_f16xi4_step_276(device, token);
  rope_step_277(device, token, last_token);
  mvm_f16xi4_step_278(device, token);
  rope_step_279(device, token, last_token);
  kvcache2hbm_step_280(device, token, last_token);
  mvm_f16xf16_step_281(device, token, last_token);
  elementwise_step_282(device);
  softmax_step_283(device, token, last_token);
  mvm_f16xi4_step_284(device, token);
  kvcache2hbm_step_285(device, token, last_token);
  mvm_f16xf16_step_286(device, token, last_token);
  mvm_f16xi4_step_287(device, token);
  elementwise_step_288(device, token);
  norm_step_289(device, token);
  mvm_f16xi4_step_290(device, token);
  activate_step_291(device, token);
  mvm_f16xi4_step_292(device, token);
  elementwise_step_293(device, token);
  mvm_f16xi4_step_294(device, token);
  elementwise_step_295(device, token);
  norm_step_296(device, token);
  mvm_f16xi4_step_297(device, token);
  rope_step_298(device, token, last_token);
  mvm_f16xi4_step_299(device, token);
  rope_step_300(device, token, last_token);
  kvcache2hbm_step_301(device, token, last_token);
  mvm_f16xf16_step_302(device, token, last_token);
  elementwise_step_303(device);
  softmax_step_304(device, token, last_token);
  mvm_f16xi4_step_305(device, token);
  kvcache2hbm_step_306(device, token, last_token);
  mvm_f16xf16_step_307(device, token, last_token);
  mvm_f16xi4_step_308(device, token);
  elementwise_step_309(device, token);
  norm_step_310(device, token);
  mvm_f16xi4_step_311(device, token);
  activate_step_312(device, token);
  mvm_f16xi4_step_313(device, token);
  elementwise_step_314(device, token);
  mvm_f16xi4_step_315(device, token);
  elementwise_step_316(device, token);
  norm_step_317(device, token);
  mvm_f16xi4_step_318(device, token);
  rope_step_319(device, token, last_token);
  mvm_f16xi4_step_320(device, token);
  rope_step_321(device, token, last_token);
  kvcache2hbm_step_322(device, token, last_token);
  mvm_f16xf16_step_323(device, token, last_token);
  elementwise_step_324(device);
  softmax_step_325(device, token, last_token);
  mvm_f16xi4_step_326(device, token);
  kvcache2hbm_step_327(device, token, last_token);
  mvm_f16xf16_step_328(device, token, last_token);
  mvm_f16xi4_step_329(device, token);
  elementwise_step_330(device, token);
  norm_step_331(device, token);
  mvm_f16xi4_step_332(device, token);
  activate_step_333(device, token);
  mvm_f16xi4_step_334(device, token);
  elementwise_step_335(device, token);
  mvm_f16xi4_step_336(device, token);
  elementwise_step_337(device, token);
  norm_step_338(device, token);
  mvm_f16xi4_step_339(device, token);
  rope_step_340(device, token, last_token);
  mvm_f16xi4_step_341(device, token);
  rope_step_342(device, token, last_token);
  kvcache2hbm_step_343(device, token, last_token);
  mvm_f16xf16_step_344(device, token, last_token);
  elementwise_step_345(device);
  softmax_step_346(device, token, last_token);
  mvm_f16xi4_step_347(device, token);
  kvcache2hbm_step_348(device, token, last_token);
  mvm_f16xf16_step_349(device, token, last_token);
  mvm_f16xi4_step_350(device, token);
  elementwise_step_351(device, token);
  norm_step_352(device, token);
  mvm_f16xi4_step_353(device, token);
  activate_step_354(device, token);
  mvm_f16xi4_step_355(device, token);
  elementwise_step_356(device, token);
  mvm_f16xi4_step_357(device, token);
  elementwise_step_358(device, token);
  norm_step_359(device, token);
  mvm_f16xi4_step_360(device, token);
  rope_step_361(device, token, last_token);
  mvm_f16xi4_step_362(device, token);
  rope_step_363(device, token, last_token);
  kvcache2hbm_step_364(device, token, last_token);
  mvm_f16xf16_step_365(device, token, last_token);
  elementwise_step_366(device);
  softmax_step_367(device, token, last_token);
  mvm_f16xi4_step_368(device, token);
  kvcache2hbm_step_369(device, token, last_token);
  mvm_f16xf16_step_370(device, token, last_token);
  mvm_f16xi4_step_371(device, token);
  elementwise_step_372(device, token);
  norm_step_373(device, token);
  mvm_f16xi4_step_374(device, token);
  activate_step_375(device, token);
  mvm_f16xi4_step_376(device, token);
  elementwise_step_377(device, token);
  mvm_f16xi4_step_378(device, token);
  elementwise_step_379(device, token);
  norm_step_380(device, token);
  mvm_f16xi4_step_381(device, token);
  rope_step_382(device, token, last_token);
  mvm_f16xi4_step_383(device, token);
  rope_step_384(device, token, last_token);
  kvcache2hbm_step_385(device, token, last_token);
  mvm_f16xf16_step_386(device, token, last_token);
  elementwise_step_387(device);
  softmax_step_388(device, token, last_token);
  mvm_f16xi4_step_389(device, token);
  kvcache2hbm_step_390(device, token, last_token);
  mvm_f16xf16_step_391(device, token, last_token);
  mvm_f16xi4_step_392(device, token);
  elementwise_step_393(device, token);
  norm_step_394(device, token);
  mvm_f16xi4_step_395(device, token);
  activate_step_396(device, token);
  mvm_f16xi4_step_397(device, token);
  elementwise_step_398(device, token);
  mvm_f16xi4_step_399(device, token);
  elementwise_step_400(device, token);
  norm_step_401(device, token);
  mvm_f16xi4_step_402(device, token);
  rope_step_403(device, token, last_token);
  mvm_f16xi4_step_404(device, token);
  rope_step_405(device, token, last_token);
  kvcache2hbm_step_406(device, token, last_token);
  mvm_f16xf16_step_407(device, token, last_token);
  elementwise_step_408(device);
  softmax_step_409(device, token, last_token);
  mvm_f16xi4_step_410(device, token);
  kvcache2hbm_step_411(device, token, last_token);
  mvm_f16xf16_step_412(device, token, last_token);
  mvm_f16xi4_step_413(device, token);
  elementwise_step_414(device, token);
  norm_step_415(device, token);
  mvm_f16xi4_step_416(device, token);
  activate_step_417(device, token);
  mvm_f16xi4_step_418(device, token);
  elementwise_step_419(device, token);
  mvm_f16xi4_step_420(device, token);
  elementwise_step_421(device, token);
  norm_step_422(device, token);
  mvm_f16xi4_step_423(device, token);
  rope_step_424(device, token, last_token);
  mvm_f16xi4_step_425(device, token);
  rope_step_426(device, token, last_token);
  kvcache2hbm_step_427(device, token, last_token);
  mvm_f16xf16_step_428(device, token, last_token);
  elementwise_step_429(device);
  softmax_step_430(device, token, last_token);
  mvm_f16xi4_step_431(device, token);
  kvcache2hbm_step_432(device, token, last_token);
  mvm_f16xf16_step_433(device, token, last_token);
  mvm_f16xi4_step_434(device, token);
  elementwise_step_435(device, token);
  norm_step_436(device, token);
  mvm_f16xi4_step_437(device, token);
  activate_step_438(device, token);
  mvm_f16xi4_step_439(device, token);
  elementwise_step_440(device, token);
  mvm_f16xi4_step_441(device, token);
  elementwise_step_442(device, token);
  norm_step_443(device, token);
  mvm_f16xi4_step_444(device, token);
  rope_step_445(device, token, last_token);
  mvm_f16xi4_step_446(device, token);
  rope_step_447(device, token, last_token);
  kvcache2hbm_step_448(device, token, last_token);
  mvm_f16xf16_step_449(device, token, last_token);
  elementwise_step_450(device);
  softmax_step_451(device, token, last_token);
  mvm_f16xi4_step_452(device, token);
  kvcache2hbm_step_453(device, token, last_token);
  mvm_f16xf16_step_454(device, token, last_token);
  mvm_f16xi4_step_455(device, token);
  elementwise_step_456(device, token);
  norm_step_457(device, token);
  mvm_f16xi4_step_458(device, token);
  activate_step_459(device, token);
  mvm_f16xi4_step_460(device, token);
  elementwise_step_461(device, token);
  mvm_f16xi4_step_462(device, token);
  elementwise_step_463(device, token);
  norm_step_464(device, token);
  mvm_f16xi4_step_465(device, token);
  rope_step_466(device, token, last_token);
  mvm_f16xi4_step_467(device, token);
  rope_step_468(device, token, last_token);
  kvcache2hbm_step_469(device, token, last_token);
  mvm_f16xf16_step_470(device, token, last_token);
  elementwise_step_471(device);
  softmax_step_472(device, token, last_token);
  mvm_f16xi4_step_473(device, token);
  kvcache2hbm_step_474(device, token, last_token);
  mvm_f16xf16_step_475(device, token, last_token);
  mvm_f16xi4_step_476(device, token);
  elementwise_step_477(device, token);
  norm_step_478(device, token);
  mvm_f16xi4_step_479(device, token);
  activate_step_480(device, token);
  mvm_f16xi4_step_481(device, token);
  elementwise_step_482(device, token);
  mvm_f16xi4_step_483(device, token);
  elementwise_step_484(device, token);
  norm_step_485(device, token);
  mvm_f16xi4_step_486(device, token);
  rope_step_487(device, token, last_token);
  mvm_f16xi4_step_488(device, token);
  rope_step_489(device, token, last_token);
  kvcache2hbm_step_490(device, token, last_token);
  mvm_f16xf16_step_491(device, token, last_token);
  elementwise_step_492(device);
  softmax_step_493(device, token, last_token);
  mvm_f16xi4_step_494(device, token);
  kvcache2hbm_step_495(device, token, last_token);
  mvm_f16xf16_step_496(device, token, last_token);
  mvm_f16xi4_step_497(device, token);
  elementwise_step_498(device, token);
  norm_step_499(device, token);
  mvm_f16xi4_step_500(device, token);
  activate_step_501(device, token);
  mvm_f16xi4_step_502(device, token);
  elementwise_step_503(device, token);
  mvm_f16xi4_step_504(device, token);
  elementwise_step_505(device, token);
  norm_step_506(device, token);
  mvm_f16xi4_step_507(device, token);
  rope_step_508(device, token, last_token);
  mvm_f16xi4_step_509(device, token);
  rope_step_510(device, token, last_token);
  kvcache2hbm_step_511(device, token, last_token);
  mvm_f16xf16_step_512(device, token, last_token);
  elementwise_step_513(device);
  softmax_step_514(device, token, last_token);
  mvm_f16xi4_step_515(device, token);
  kvcache2hbm_step_516(device, token, last_token);
  mvm_f16xf16_step_517(device, token, last_token);
  mvm_f16xi4_step_518(device, token);
  elementwise_step_519(device, token);
  norm_step_520(device, token);
  mvm_f16xi4_step_521(device, token);
  activate_step_522(device, token);
  mvm_f16xi4_step_523(device, token);
  elementwise_step_524(device, token);
  mvm_f16xi4_step_525(device, token);
  elementwise_step_526(device, token);
  norm_step_527(device, token);
  mvm_f16xi4_step_528(device, token);
  rope_step_529(device, token, last_token);
  mvm_f16xi4_step_530(device, token);
  rope_step_531(device, token, last_token);
  kvcache2hbm_step_532(device, token, last_token);
  mvm_f16xf16_step_533(device, token, last_token);
  elementwise_step_534(device);
  softmax_step_535(device, token, last_token);
  mvm_f16xi4_step_536(device, token);
  kvcache2hbm_step_537(device, token, last_token);
  mvm_f16xf16_step_538(device, token, last_token);
  mvm_f16xi4_step_539(device, token);
  elementwise_step_540(device, token);
  norm_step_541(device, token);
  mvm_f16xi4_step_542(device, token);
  activate_step_543(device, token);
  mvm_f16xi4_step_544(device, token);
  elementwise_step_545(device, token);
  mvm_f16xi4_step_546(device, token);
  elementwise_step_547(device, token);
  norm_step_548(device, token);
  mvm_f16xi4_step_549(device, token);
  rope_step_550(device, token, last_token);
  mvm_f16xi4_step_551(device, token);
  rope_step_552(device, token, last_token);
  kvcache2hbm_step_553(device, token, last_token);
  mvm_f16xf16_step_554(device, token, last_token);
  elementwise_step_555(device);
  softmax_step_556(device, token, last_token);
  mvm_f16xi4_step_557(device, token);
  kvcache2hbm_step_558(device, token, last_token);
  mvm_f16xf16_step_559(device, token, last_token);
  mvm_f16xi4_step_560(device, token);
  elementwise_step_561(device, token);
  norm_step_562(device, token);
  mvm_f16xi4_step_563(device, token);
  activate_step_564(device, token);
  mvm_f16xi4_step_565(device, token);
  elementwise_step_566(device, token);
  mvm_f16xi4_step_567(device, token);
  elementwise_step_568(device, token);
  norm_step_569(device, token);
  mvm_f16xi4_step_570(device, token);
  rope_step_571(device, token, last_token);
  mvm_f16xi4_step_572(device, token);
  rope_step_573(device, token, last_token);
  kvcache2hbm_step_574(device, token, last_token);
  mvm_f16xf16_step_575(device, token, last_token);
  elementwise_step_576(device);
  softmax_step_577(device, token, last_token);
  mvm_f16xi4_step_578(device, token);
  kvcache2hbm_step_579(device, token, last_token);
  mvm_f16xf16_step_580(device, token, last_token);
  mvm_f16xi4_step_581(device, token);
  elementwise_step_582(device, token);
  norm_step_583(device, token);
  mvm_f16xi4_step_584(device, token);
  activate_step_585(device, token);
  mvm_f16xi4_step_586(device, token);
  elementwise_step_587(device, token);
  mvm_f16xi4_step_588(device, token);
  elementwise_step_589(device, token);
  norm_step_590(device, token);
  mvm_f16xi4_step_591(device, token);
  rope_step_592(device, token, last_token);
  mvm_f16xi4_step_593(device, token);
  rope_step_594(device, token, last_token);
  kvcache2hbm_step_595(device, token, last_token);
  mvm_f16xf16_step_596(device, token, last_token);
  elementwise_step_597(device);
  softmax_step_598(device, token, last_token);
  mvm_f16xi4_step_599(device, token);
  kvcache2hbm_step_600(device, token, last_token);
  mvm_f16xf16_step_601(device, token, last_token);
  mvm_f16xi4_step_602(device, token);
  elementwise_step_603(device, token);
  norm_step_604(device, token);
  mvm_f16xi4_step_605(device, token);
  activate_step_606(device, token);
  mvm_f16xi4_step_607(device, token);
  elementwise_step_608(device, token);
  mvm_f16xi4_step_609(device, token);
  elementwise_step_610(device, token);
  norm_step_611(device, token);
  mvm_f16xi4_step_612(device, token);
  rope_step_613(device, token, last_token);
  mvm_f16xi4_step_614(device, token);
  rope_step_615(device, token, last_token);
  kvcache2hbm_step_616(device, token, last_token);
  mvm_f16xf16_step_617(device, token, last_token);
  elementwise_step_618(device);
  softmax_step_619(device, token, last_token);
  mvm_f16xi4_step_620(device, token);
  kvcache2hbm_step_621(device, token, last_token);
  mvm_f16xf16_step_622(device, token, last_token);
  mvm_f16xi4_step_623(device, token);
  elementwise_step_624(device, token);
  norm_step_625(device, token);
  mvm_f16xi4_step_626(device, token);
  activate_step_627(device, token);
  mvm_f16xi4_step_628(device, token);
  elementwise_step_629(device, token);
  mvm_f16xi4_step_630(device, token);
  elementwise_step_631(device, token);
  norm_step_632(device, token);
  mvm_f16xi4_step_633(device, token);
  rope_step_634(device, token, last_token);
  mvm_f16xi4_step_635(device, token);
  rope_step_636(device, token, last_token);
  kvcache2hbm_step_637(device, token, last_token);
  mvm_f16xf16_step_638(device, token, last_token);
  elementwise_step_639(device);
  softmax_step_640(device, token, last_token);
  mvm_f16xi4_step_641(device, token);
  kvcache2hbm_step_642(device, token, last_token);
  mvm_f16xf16_step_643(device, token, last_token);
  mvm_f16xi4_step_644(device, token);
  elementwise_step_645(device, token);
  norm_step_646(device, token);
  mvm_f16xi4_step_647(device, token);
  activate_step_648(device, token);
  mvm_f16xi4_step_649(device, token);
  elementwise_step_650(device, token);
  mvm_f16xi4_step_651(device, token);
  elementwise_step_652(device, token);
  norm_step_653(device, token);
  mvm_f16xi4_step_654(device, token);
  rope_step_655(device, token, last_token);
  mvm_f16xi4_step_656(device, token);
  rope_step_657(device, token, last_token);
  kvcache2hbm_step_658(device, token, last_token);
  mvm_f16xf16_step_659(device, token, last_token);
  elementwise_step_660(device);
  softmax_step_661(device, token, last_token);
  mvm_f16xi4_step_662(device, token);
  kvcache2hbm_step_663(device, token, last_token);
  mvm_f16xf16_step_664(device, token, last_token);
  mvm_f16xi4_step_665(device, token);
  elementwise_step_666(device, token);
  norm_step_667(device, token);
  mvm_f16xi4_step_668(device, token);
  activate_step_669(device, token);
  mvm_f16xi4_step_670(device, token);
  elementwise_step_671(device, token);
  mvm_f16xi4_step_672(device, token);
  elementwise_step_673(device, token);
}
